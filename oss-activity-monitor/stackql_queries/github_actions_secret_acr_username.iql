/*+ preflight */
SELECT COUNT(*) as count FROM github.actions.secrets
WHERE owner = '{{ github_repo_owner }}' 
AND repo = '{{ github_repo_name }}'
AND secret_name = '{{ github_repo_secret_name }}'

/*+ postdeploy, retries=5, retry_delay=5 */
SELECT COUNT(*) as count FROM github.actions.secrets
WHERE owner = '{{ github_repo_owner }}' 
AND repo = '{{ github_repo_name }}'
AND secret_name = '{{ github_repo_secret_name }}'